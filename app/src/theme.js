import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
	success: {
	  main:  'rgb(99, 160, 76)', // Replace with your desired color
	},
  },
});


export default theme;